{
  "name": "web",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "uv venv --clear && uv pip install -r requirements.txt",
    "dev": ".venv/bin/python -m streamlit run app.py --server.port=8501 --server.address=0.0.0.0 --server.headless=true"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}