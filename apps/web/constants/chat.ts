/**
 * Chat & AI tuning constants.
 */

/** Debounce delay (ms) before auto-scrolling after a message update */
export const AUTO_SCROLL_DEBOUNCE_MS = 300;

/** Default number of citation pills shown before collapsing */
export const PILL_VISIBLE_DEFAULT = 3;

/** Default LLM temperature for chat completions */
export const CHAT_TEMPERATURE = 0.7;

/** Default max tokens for chat completions */
export const CHAT_MAX_TOKENS = 1000;
