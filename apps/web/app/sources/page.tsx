import { SourcesLayout } from '@/components/features/sources/sources-layout';

export const metadata = {
  title: 'Data Sources | XHIVE',
  description: 'Manage and ingest SharePoint documents into XHIVE.',
};

export default function SourcesPage() {
  return <SourcesLayout />;
}
