{
  "name": "ai",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "uv venv --clear && uv pip install -r requirements.txt",
    "dev": "source .venv/bin/activate && uvicorn main:app --reload",
    "start": "source .venv/bin/activate && uvicorn main:app --reload"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}