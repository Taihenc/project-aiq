{
  "name": "chat-service",
  "version": "1.0.0",
  "description": "Chat Service for session management and conversation history",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "uv sync",
    "dev": "source .venv/bin/activate && uvicorn main:app --reload --port 8002",
    "start": "source .venv/bin/activate && uvicorn main:app --port 8002"
  },
  "keywords": ["chat", "session", "history", "fastapi"],
  "author": "",
  "license": "ISC"
}
